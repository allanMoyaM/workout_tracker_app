import 'package:flutter/material.dart';

// Brand constants — never change between themes
class AppColors {
  static const Color accent = Color(0xFFF5A520);
  static const Color accentBlue = Color(0xFF1565C0);
}
